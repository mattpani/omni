plugins {
    id 'org.springframework.boot' version "${spring}"
}
dependencies {
    implementation project(':omni-persistence')
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}